# Kundali Reference Data

This directory intentionally contains schema-only reference contracts for future validation datasets.

The current Kundali implementation is identified as `internal_approx_v1` with `accuracyClass=APPROXIMATE` and `validationStatus=UNVALIDATED`. It does not use Swiss Ephemeris data files and this repository does not ship authoritative numeric astrology fixtures.
